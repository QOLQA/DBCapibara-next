"use client";

/**
 * Sidebar content for the comparison page.
 * Placeholder ready to extend with comparison-specific UI (e.g. version diff, expand).
 */
export function ComparisonSidebarContent() {
	return (
		<div className="flex flex-col h-full w-full gap-4 text-secondary-white">
			<div className="text-p">Compare schema versions and metrics here.</div>
		</div>
	);
}
